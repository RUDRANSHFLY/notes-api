[project]
name = "notes-api-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "RUDRANSH BARADIYA", email = "mayamayavir@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.140.13",
    "sqlalchemy>=2.0.51",
]

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
